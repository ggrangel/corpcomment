import { type ReactElement } from "react";

export default function Logo(): ReactElement {
  return (
    <a href="/" className="logo">
      <img src="https://bytegrad.com/course-assets/js/1/logo.svg" />
    </a>
  );
}
