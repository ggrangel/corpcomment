import { type ReactElement } from "react";

export default function Pattern(): ReactElement {
  return (
    <img
      src="https://bytegrad.com/course-assets/js/1/pattern.svg"
      alt="pattern"
      className="pattern"
    />
  );
}
